# Quick Start Guide

## 🚀 Run Application in 3 Steps

### Step 1: Start Development Server
```bash
cd c:\Code\BookShelfUI\bookshelf-app
npm start
```

### Step 2: Open Browser
```
http://localhost:4200
```

### Step 3: Connect Your API
Update `src/environments/environment.ts`:
```typescript
export const environment = {
  production: false,
  apiUrl: 'https://localhost:5001/api',  // ← Your .NET API URL
};
```

---

## 📋 Project Structure

```
bookshelf-app/
├── src/app/
│   ├── store/              # ← NGXS State Management
│   ├── services/           # ← API Communication
│   ├── components/         # ← UI Components
│   ├── models/             # ← TypeScript Interfaces
│   └── environments/       # ← Configuration
├── angular.json            # ← Build Config
└── package.json            # ← Dependencies
```

---

## 🔧 Common Commands

| Command | Description |
|---------|-------------|
| `npm start` | Start dev server |
| `npm run build` | Production build |
| `npm test` | Run tests |
| `ng generate component` | Create component |
| `ng generate service` | Create service |

---

## 📊 State Management Flow

```
Component Action
    ↓
Store Dispatch
    ↓
API Call
    ↓
State Update
    ↓
UI Render
```

---

## ✅ Verify Installation

1. `npm start` runs without errors ✅
2. Browser opens to `http://localhost:4200` ✅
3. Page shows navigation and empty book list ✅
4. No console errors ✅

---

## 🔗 Required .NET API Endpoints

Your API must have these routes:

```
GET    /api/books           # Get all
GET    /api/books/{id}      # Get one
POST   /api/books           # Create
PUT    /api/books/{id}      # Update
DELETE /api/books/{id}      # Delete
```

---

## 📚 Components

### BookListComponent
- Displays all books
- Search & filter
- Sort options
- Delete books

### BookFormComponent
- Add new book
- Edit existing book
- Form validation
- Error messages

---

## 🎨 Styling

- **Primary Color**: `#4299e1` (Blue)
- **Danger Color**: `#e53e3e` (Red)
- **Background**: `#f7fafc` (Light Gray)

Global styles in: `src/app/app.scss`
Component styles: `*.component.scss`

---

## 🐛 Debugging

### See State Changes
Open **Redux DevTools** browser extension (if installed)

### See Console Logs
All actions logged to browser console in development

### Network Requests
Check **Network tab** in browser DevTools

---

## 📁 Key Files to Know

| File | Purpose |
|------|---------|
| `book.state.ts` | Where state updates happen |
| `book.actions.ts` | All actions you can dispatch |
| `book.service.ts` | API calls to your backend |
| `book-list.component.ts` | List view logic |
| `book-form.component.ts` | Form logic |

---

## 🚨 Common Issues

### "Cannot connect to API"
→ Ensure .NET API is running on `https://localhost:5001`

### "State not updating"
→ Check Redux DevTools to see if action was dispatched

### "Form validation errors not showing"
→ Make sure field is touched: `field.markAsTouched()`

### "Build errors"
→ Run `npm install` to ensure dependencies are installed

---

## 💡 Tips

1. Use `ng serve --open` to auto-open browser
2. Use `ng serve --port 4300` for different port
3. Check `.env` file for environment variables
4. Use Postman to test your API endpoints first
5. Use browser DevTools to inspect component state

---

## 📚 Documentation Files

- `PROJECT_SUMMARY.md` - Complete project overview
- `SETUP_GUIDE.md` - Detailed setup instructions
- `README.md` - Generated by Angular

---

## ✨ You're Ready!

Your Angular + NGXS + REST API application is fully configured.

**Next**: Connect your .NET Core 10 API and test the CRUD operations.

---

**Questions?** Check the documentation files above!
